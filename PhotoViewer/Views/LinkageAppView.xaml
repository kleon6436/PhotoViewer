<Page x:Class="PhotoViewer.Views.LinkageAppView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="600"
      Title="LinkageAppView">
    <Page.Background>
        <SolidColorBrush Color="#F0F0F0" />
    </Page.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10" Orientation="Vertical">
            <TextBlock Text="■ 連携アプリ設定 ■" FontSize="24" FontWeight="Medium" Margin="0,0,0,15"/>
            <TextBlock Text="連携アプリを1つ登録することができます。" FontSize="15" Margin="10,0,0,5"/>
            <TextBlock Text="登録したアプリは、コンテンツリストの画像上で右クリックすると、起動することができます。" FontSize="15" Margin="10,0,0,5"/>
            <TextBlock Text="登録するアプリを以下の参照ボタンより選択してください。" FontSize="15" Margin="10,0,0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox VerticalAlignment="Center" VerticalContentAlignment="Center" IsReadOnly="True" Text="{Binding LinkAppPath}" Height="40" Width="350" FontSize="14"/>
            <Button Content="参照" FontSize="18" Command="{Binding LinkAppReferenceCommand, Mode=OneTime}" VerticalAlignment="Center" Height="40" Width="60" Margin="10,0,0,0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,20,0">
            <Button Content="登録" Command="{Binding RegisterLinkAppCommand, Mode=OneTime}" Width="85" Height="40" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0"/>
            <Button Content="削除" Command="{Binding DeleteLinkAppCommand, Mode=OneTime}" Width="85" Height="40" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,20,0"/>
        </StackPanel>
    </Grid>
</Page>
